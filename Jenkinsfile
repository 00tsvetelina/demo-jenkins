pipeline {

    agent any

    stages {

        stage("build") {
            steps {
                echo 'building the applocation...'
            }
        }

        stage("tets") {
            steps {
                echo 'testing the applocation...'
            }
        }
        
        stage("deploy") {
            steps {
                echo 'deploying the applocation...'
            }
        }

    }
}